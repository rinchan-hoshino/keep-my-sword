package dev.rinchan.keepmysword.client;

/** Visual language for broken items inside inventory-like GUI slots. */
public final class BrokenVisualStyle {
    /** A light warning red that leaves the original icon readable. */
    public static final int SLOT_OVERLAY_ARGB = 0x33FF6B6B;

    private BrokenVisualStyle() {
    }
}
